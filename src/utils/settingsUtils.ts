export const defaultSettings = {
    weatherUpdateInterval: 30, // Update weather every 30 minutes
    backgroundImageInterval: 5, // Update background image ever 5 minutes
    use12HourMode: false
}
